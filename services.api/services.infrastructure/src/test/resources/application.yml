spring:
  profiles:
    active: local
axon:
  mongo:
    token-store:
      enabled: false
    saga-store:
      enabled: false
    event-store:
      enabled: false
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              ReactiveMongoTemplate: DEBUG
